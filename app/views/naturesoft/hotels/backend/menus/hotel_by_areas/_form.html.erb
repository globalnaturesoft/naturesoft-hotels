<% menu = Naturesoft::Menus::Menu.where(id: params["hotel_list_id"]).first %>
<div class="form-group">
    <label class="col-md-2 control-label" ><%= t(:hotel_list_id) %> <span class='text-danger'>*</span></label>
    <div class="col-md-10">
        <% selected = options_for_select([[menu.name,menu.id]],menu.id) if !menu.nil? %>
        <%= select_tag("params[hotel_list_id]",
          selected,
          {
            class: "select2-ajax required",
            "data-url": naturesoft_menus.select2_backend_menus_path(parent_id: "all"),
          }
        ) %>
    </div>
</div>
